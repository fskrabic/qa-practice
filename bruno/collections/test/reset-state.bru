meta {
  name: Reset state
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/test/reset
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", "");
  }
}

assert {
  res.status: 200
}