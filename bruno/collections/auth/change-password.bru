meta {
  name: Change password
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/auth/password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "currentPassword": "admin123",
    "newPassword": "admin123"
  }
}

assert {
  res.status: 200
}